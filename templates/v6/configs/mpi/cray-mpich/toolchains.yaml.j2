# Rendered for {{ profile.system.name }} / Cray MPICH toolchains (from mpi_providers)
toolchains:
{% for toolchain in mpi_plan["cray-mpich"].toolchains %}
  {{ toolchain.name }}:
{% for entry in toolchain.entries %}
    - spec: "{{ entry.spec }}"
      when: "{{ entry.when }}"
{% endfor %}
{% endfor %}
